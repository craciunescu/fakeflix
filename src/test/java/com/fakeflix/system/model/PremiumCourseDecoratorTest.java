package com.fakeflix.system.model;

/*import static org.assertj.core.api.Assertions.assertThat;

import java.util.List;

import com.fakeflix.system.domain.*;

import org.junit.jupiter.api.Test;*/

public class PremiumCourseDecoratorTest {

    /*
     * @Test void shouldInitialize() { final Author author = new Author("John",
     * "Doe");
     * 
     * final Media baseCourse = new Movie("FILM1", Pegi.ALLPUBLIC,
     * List.of(Tag.ACTION, Tag.HORROR), author);
     * 
     * final PremiumMovieDecorator premiumCourse = new
     * PremiumMovieDecorator(baseCourse);
     * 
     * assertThat(premiumCourse).isNotNull();
     * assertThat(premiumCourse.getAuthor()).isNotNull();
     * assertThat(premiumCourse.getPegi()).isNotNull();
     * assertThat(premiumCourse.getName()).isNotBlank();
     * assertThat(premiumCourse.getTags()).contains(Tag.ACTION);
     * assertThat(premiumCourse.getTags()).contains(Tag.HORROR);
     * 
     * System.out.println(premiumCourse.getTags()); }
     */

}
